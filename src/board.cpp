#include "include/board.h"
#include "include/engine.h"
#include <algorithm>
#include <iostream>

Board::Board(int initialWidth, int initialHeigth): width(initialWidth), heigth(initialHeigth){
 
}

Sprite* Board::Init(Engine* engine){
   return nullptr;
};

